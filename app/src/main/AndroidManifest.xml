<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.gb.weatherapprebuild">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinApp">
        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAhTwFjBWRriMF-uMpDH40PwQTc-cW4zYk" />

        <provider
            android:name=".repository.contentprovider.EducationContentProvider"
            android:authorities="@string/authorities"
            android:enabled="true"
            android:exported="true" />
        <service android:name=".view.details.DetailsService" /> <!-- <receiver android:name=".view.test.MainBroadcastReceiver" -->

        <service android:name=".view.test.MainService" />
    </application>

</manifest>